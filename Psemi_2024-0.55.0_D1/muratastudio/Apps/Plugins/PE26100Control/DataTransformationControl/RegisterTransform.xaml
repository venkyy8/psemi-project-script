<UserControl x:Class="ARCxCCxxControl.DataTransformationControl.RegisterTransform"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="161">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border BorderThickness="1" BorderBrush="#FFBBBBBB" CornerRadius="4">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="txtFormatted" Width="80" Text="Test" />
                <Border BorderThickness="1" BorderBrush="#FFBBBBBB" Width="1"/>
                <TextBlock x:Name="txtRegister"  Width="80" Text="{Binding Path=RegisterText, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}}"/>
        </StackPanel>
        </Border>
    </Grid>
</UserControl>
